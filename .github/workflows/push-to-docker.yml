# # name: Build and Push to Docker Hub

# # on:
# #   push:
# #     branches:
# #       - "feature/*" # trigger only on feature branches

# # jobs:
# #   create-pr:
# #     runs-on: ubuntu-latest

# #     steps:
# #       # Step 1: Checkout repo (like git clone)
# #       - name: Checkout code
# #       - uses: actions/checkout@v4

# #       - name: Create Pull Request
# #         uses: peter-evans/create-pull-request@v7
# #         with:
# #           token: ${{ secrets.GITHUB_TOKEN }}
# #           branch: ${{ github.ref_name }} # current feature branch
# #           base: main # target branch
# #           title: "Auto PR: ${{ github.ref_name }} → main"
# #           body: |
# #             This PR was automatically created from branch **${{ github.ref_name }}** to **main**.

# name: Build and Push to Docker Hub

# on:
#   push:
#     branches:
#       - main # Runs only when you push to main branch

# jobs:
#   docker:
#     runs-on: ubuntu-latest

#     steps:
#       # Step 1: Checkout repo (like git clone)
#       - name: Checkout code
#         uses: actions/checkout@v3

#       # Step 2: Log in to Docker Hub
#       - name: Log in to Docker Hub
#         uses: docker/login-action@v3
#         with:
#           username: ${{ secrets.DOCKERHUB_USERNAME }}
#           password: ${{ secrets.DOCKERHUB_PASSWORD }}

#       # Step 3: Build Docker image
#       - name: Build Docker image
#         run: docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/myapp:latest .

#       # Step 4: Push Docker image
#       - name: Push Docker image
#         run: docker push ${{ secrets.DOCKERHUB_USERNAME }}/myapp:latest

name: Deploy to Local via SSH

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to Mac via SSH
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.HOST }}
          port: ${{ secrets.PORT }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.PRIVATE_KEY }}
          passphrase: ""
          debug: true
          script: |
            export PATH=$PATH:/usr/local/bin:/opt/homebrew/bin

            echo "Connected to server"

            cd /Users/sotsys033/Desktop/CICD_TEST

            echo "Pull latest code"
            git pull origin main

            echo "Build Docker image"
            docker build -t my-app .

            echo "Stop old container"
            docker stop my-container || true

            echo "Remove old container"
            docker rm my-container || true

            echo "Run new container"
            docker run -d -p 5858:3000 --name my-container my-app

            echo "Deployment complete"

            echo "SSH connection closed"
